
{
  "buildCommand": "cd nextjs_space && prisma generate && next build",
  "framework": "nextjs",
  "installCommand": "cd nextjs_space && npm install --legacy-peer-deps && npm install -g prisma",
  "regions": ["iad1"]
}
